#!/usr/bin/env bash

while true; do
    $SSE_GATEWAY_ROOT/scripts/run-gateway.sh --callback-url http://localhost:{{ backend_port }}/api/sse/callback --port {{ sse_gateway_port }}

    echo
    echo "Press any key to restart the server..."
    echo

    read -n1 -s
done
