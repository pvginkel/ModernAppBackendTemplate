"""Database models.
{% if use_database %}
Import all models here so they are registered with SQLAlchemy
when the app starts.

Example:
    from app.models.item import Item

    __all__ = ["Item"]
{% else %}
Database support is not enabled for this project.
Enable it by regenerating with use_database=true.
{% endif %}
"""
